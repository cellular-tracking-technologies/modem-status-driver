{
  "name": "modem-status-driver",
  "version": "0.0.1",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "build": "babel src -d dist",
    "compile": "babel src -d dist --source-maps --watch",
    "test": "node dist/test/index.js"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  },
  "dependencies": {
    "serialport": "^8.0.7",
    "uuid": "^7.0.1"
  }
}
